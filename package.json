{
  "name": "theft-timer",
  "version": "2.0.0",
  "private": true,
  "description": "Stolen Minutes PWA with simple spoken diabetes capture",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
